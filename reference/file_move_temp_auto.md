# Move temporary file automatically from the R console

It works well when you have no API to download a file, but still want a
fast R implementation.

## Usage

``` r
file_move_temp_auto(destdir)
```

## Arguments

- destdir:

  The desired directory to send this to

## See also

[`file_rename_auto()`](https://olivroy.github.io/reuseme/reference/file_rename_auto.md)
